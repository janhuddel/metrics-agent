{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug metrics-agent",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/metrics-agent",
            "cwd": "${workspaceFolder}",
            "env": {
                "DEBUG": "1"
            }        
        }
    ]
}